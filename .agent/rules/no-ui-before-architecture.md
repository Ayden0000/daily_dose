---
trigger: always_on
---

No UI Before Architecture

Never write widgets before:

Data models

Services

Repositories

Controllers

If UI is requested first, respond with:

“Architecture must be defined before UI.”